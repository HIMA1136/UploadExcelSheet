<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dropzone with Table</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <style>
        .dropzone {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: center;
            height: auto;
            min-height: 150px;
            border: 2px dashed #007bff;
            border-radius: 5px;
            background-color: #f8f9fa;
            color: #6c757d;
            padding: 10px;
            cursor: pointer;
        }

            .dropzone:hover {
                background-color: #e9ecef;
            }

        .file-box {
            display: inline-block;
            padding: 10px;
            margin: 5px;
            background-color: #e9ecef;
            border: 1px solid #ced4da;
            border-radius: 5px;
            font-size: 14px;
            color: #495057;
            max-width: 150px;
            text-align: center;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            cursor: pointer;
        }

            .file-box:hover {
                background-color: #007bff;
                color: white;
            }

        .export-btn {
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
            font-size: 16px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

            .export-btn:hover {
                background-color: #0056b3;
            }

        #file-table .total-row {
            background-color: #4caf50; /* Green background */
            color: white; /* White text for contrast */
            font-weight: bold; /* Make text bold */
        }

            /* Distinct color for "Total Value After Taxing" and "Total Value before Taxing" cells */
            #file-table .total-row td:last-child {
                background-color: #388e3c; /* Darker green background for the last cell */
            }
    </style>
</head>
<body>
    <div class="container py-5">
        <!-- Dropzone -->
        <h5>Dropzone</h5>
        <div id="dropzone" class="dropzone">
            Drop files here or click to upload.<br>
            <small>(Click the area to select files.)</small>
        </div>

        <!-- Static Table -->
        <div class="mt-4">
            <table id="file-table" class="table table-bordered table-striped">
                <thead class="table-primary">
                    <tr id="table-headers">
                        <!-- Dynamic headers will be appended here -->
                    </tr>
                </thead>
                <tbody>
                    <!-- Dynamic rows can be added dynamically -->
                </tbody>
            </table>
        </div>

        <!-- Export Button -->
        <button id="export-btn" class="export-btn">Export</button>
    </div>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="site.js"></script>
    <script src="~/js/excelData.js"></script>
</body>
</html>
